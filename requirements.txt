# Web framework
fastapi==0.141.1
uvicorn==0.52.4

# Vector search
faiss-cpu==1.15.0

# PyTorch (CPU-only) — must use the extra index URL
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
torchvision==0.28.0+cpu

# Image & data
pillow==12.3.0
pandas==3.0.5
numpy==2.5.2

# FastAPI form/file upload support
python-multipart==0.0.32